name: Build Executables

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Release version (e.g. v1.0.0)"
        required: true
        type: string
        default: "v0.1.0"

permissions:
  contents: write
  actions: write

jobs:
  build:
    name: Build ${{ matrix.artifact }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            artifact: mirofish-linux-x64
            archive: mirofish-linux-x64.tar.gz
          - os: macos-latest
            artifact: mirofish-macos-x64
            archive: mirofish-macos-x64.tar.gz
          - os: windows-latest
            artifact: mirofish-windows-x64
            archive: mirofish-windows-x64.zip

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Build frontend
        run: |
          cd frontend
          npm ci
          npm run build

      - name: Setup uv
        uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.11"

      - name: Install Python dependencies
        working-directory: backend
        env:
          VIRTUAL_ENV: ${{ github.workspace }}/backend/.venv
        run: |
          uv sync
          uv pip install pyinstaller

      - name: Build executable
        shell: bash
        working-directory: backend
        env:
          VIRTUAL_ENV: ${{ github.workspace }}/backend/.venv
        run: |
          SEP=":"
          if [ "${{ matrix.os }}" = "windows-latest" ]; then SEP=";"; fi

          # 排除 camel-ai 传递引入但应用未使用的巨型依赖，
          # 这些包会导致 Looking for dynamic libraries 步骤卡死或超时
          EXCLUDES="
            --exclude-module torch
            --exclude-module torchvision
            --exclude-module torchaudio
            --exclude-module tensorflow
            --exclude-module tensorboard
            --exclude-module triton
            --exclude-module nvidia
            --exclude-module matplotlib
          "

          # 使用 python -m PyInstaller 确保 sys.executable 正确
          uv run python -m PyInstaller \
            --onedir \
            --noconfirm \
            --clean \
            --name mirofish \
            --add-data "../frontend/dist${SEP}frontend_dist" \
            --add-data "../static${SEP}static" \
            --add-data "../locales${SEP}locales" \
            --collect-all flask \
            --collect-all werkzeug \
            --collect-all jinja2 \
            --collect-all camel \
            --collect-all zep_cloud \
            --hidden-import flask \
            --hidden-import markupsafe \
            --hidden-import itsdangerous \
            --hidden-import blinker \
            --hidden-import click \
            $EXCLUDES \
            production.py

      - name: Package (Linux/macOS)
        if: runner.os != 'Windows'
        run: |
          cd backend/dist/mirofish
          chmod +x mirofish
          cd ..
          tar -czf "${{ matrix.archive }}" mirofish

      - name: Package (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          cd backend/dist
          7z a "${{ matrix.archive }}" mirofish > /dev/null

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: backend/dist/${{ matrix.archive }}
          if-no-files-found: error

  release:
    name: Create Release
    needs: build
    if: always() && needs.build.result == 'success'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Create tag and push
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git tag "${{ inputs.version }}"
          git push origin "${{ inputs.version }}"

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.version }}
          files: artifacts/**/*
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
